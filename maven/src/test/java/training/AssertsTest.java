package training;

import org.junit.jupiter.api.Test;

public class AssertsTest {
    @Test
    public void test() {
        /*
        int a = 2;
        int b = 2;
        String a1 = "Select";
        String b1 = "Select";
        // Assertions.assertSame(a,b);        // проверяет равенство аргументов между собой
        // Assertions.assertNotSame(a,b);           // проверяет не равенство
        // Assertions.assertEquals(a1,b1);           // проверка совпадений в строке
        //  Assertions.assertNotEquals(a1,b1, "Появляется когда тест упал");


        int[] arr = new int[]{1, 2, 3};
        int[] arr1 = new int[]{1, 2, 4};
         Assertions.assertArrayEquals(arr,arr1);         // сравнение массивов
        Коллекции
          Iterable<Integer> list1 = new ArrayList<>(Arrays.asList(1,2,3,4,5));
         Iterable<Integer> list2 = new ArrayList<>(Arrays.asList(1,2,3,4,5));

         Assertions.assertIterableEquals(list1,list2);        // сравнение численных

       ArrayList<String> lis1 = new ArrayList<>(Arrays.asList("string 1", "string2"));
       ArrayList<String> lis2 = new ArrayList<>(Arrays.asList("string 1", "string2"));
       Assertions.assertLinesMatch(lis1, lis2);     // сравнение стринговых коллекций

       Assertions.assertNull("SSS");         // проверка на пустую строку или значение.
        Assertions.assertNotNull("ssss");

        Assertions.assertTrue(true);            // проверка на true . false
       Assertions.assertFalse(false);

        assertTimeout(Duration.ofSeconds(6),()->{            // ассерт выполняет до конца всё а после проверяет за какое время выполнился тест
            System.out.println("start");
            Thread.sleep(5000);         // задержка выполнения кода
            System.out.println("finish");
        });

    Assertions.assertTimeoutPreemptively(Duration.ofMillis(500), ()-> {         // ассерт выполняет всё построчно если не вписывается в дюрэйшен бросает ошибку прерывая вополнение
        System.out.println("start");
        Thread.sleep(550);
        System.out.println("finish");
    });

        Assertions.assertThrows(NullPointerException.class, () ->
        {
            int aa = 2+2;
        });

      if (2 == 2);
        Assertions.fail();          // зафэйлить тест
        */

    }
}
